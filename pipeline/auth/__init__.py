"""CorridorIQ secure employee CRM — authentication, RBAC, sessions (Sprint 5).

Backend authorization is mandatory. Sales users never get direct DB access;
all access flows through the permission-enforcing API.
"""
